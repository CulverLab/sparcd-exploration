`Media/<sha256>/preview-640.jpg` is a 640px-long-edge JPEG at quality 0.7 beside its original.
`media.csv` continues to name the original. The tagger falls back to that original when the preview is unavailable.
